<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Forgot Password</title>
  <style>
    /* General Page Styles */
    body {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: #0d0b1a;
      background-image: url("https://www.transparenttextures.com/patterns/stardust.png");
      background-size: cover;
    }

    /* Center Card */
    .card {
      background: white;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.2);
      text-align: center;
      width: 350px;
    }

    /* Icon */
    .icon {
      font-size: 40px;
      margin-bottom: 15px;
    }

    /* Heading */
    .card h2 {
      margin: 10px 0;
      font-size: 22px;
      color: #222;
    }

    /* Subtext */
    .card p {
      margin: 5px 0 20px;
      color: #777;
      font-size: 14px;
    }

    /* Input Field */
    .card input {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 6px;
      margin-bottom: 15px;
      font-size: 14px;
    }

    /* Back link */
    .card a {
      display: block;
      margin-bottom: 15px;
      font-size: 13px;
      text-decoration: none;
      color: #555;
    }
    .card a:hover {
      text-decoration: underline;
    }

    /* Button */
    .btn {
      background: #a855f7;
      border: none;
      color: white;
      padding: 12px;
      border-radius: 8px;
      width: 100%;
      font-size: 15px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }
    .btn:hover {
      background: #9333ea;
    }
  </style>
</head>
<body>

  <div class="card">
    <div class="icon">🔑</div>
    <h2>Forgot Password?</h2>
    <p>No worries, enter your email for instructions</p>
    
    <input type="email" placeholder="Enter your email" required>
    
    <a href="#">← Return to login page</a>
    
    <button class="btn">Send 4 digit code</button>
  </div>

</body>
</html>
